# Environment variables for local development
# Backend API URL (local)
VITE_API_URL=http://localhost:3000/api

# Backend base URL for Socket.IO and static resources (local)
VITE_BACKEND_URL=http://localhost:3000
